<article>
    <div class="card-header">
    <h3>{{title}}</h3>  <h6>Posted by: {{user.username}}  @ {{formatDate created_on}}</h6>
    </div>
    <div class="card-content">
    {{text}}
    </div>
</article>